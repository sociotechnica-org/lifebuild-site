# Cloudflare Pages configuration
# This file is optional for Cloudflare Pages deployments
# Pages will use the build settings from the dashboard

name = "lifebuild-site"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
cwd = "."

[build.upload]
format = "service-worker"
dir = "dist"
